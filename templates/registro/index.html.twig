{% extends 'base.html.twig' %}

{% block title %}Registro index{% endblock %}

{% block body %}
    <h1>Registro index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Numero</th>
                <th>Lote</th>
                <th>DataRegistro</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for registro in registros %}
            <tr>
                <td>{{ registro.ticket.numero }}</td>
                <td>{{ registro.ticket.lote.id }}</td>
                <td>{{ registro.dataRegistro ? registro.dataRegistro|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_registro_show', {'id': registro.id}) }}">show</a>
                    <a href="{{ path('app_registro_edit', {'id': registro.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_registro_new') }}">Create new</a>
{% endblock %}
