{% extends 'base.html.twig' %}

{% block title %}Evento{% endblock %}
{% block header_title %} {{ evento.nome }} {% endblock %}
{% block body %}
    <h5 class="h6 mx-2"> Dados do Evento: </h5>
    <div class="card card-body">
        <table class="table">
            <tbody>
            <tr>
                <th>Código:</th>
                <td class="small text-muted">{{ evento.id }}</td>
            </tr>
            <tr>
                <th>Nome:</th>
                <td>{{ evento.nome }}</td>
            </tr>
            <tr>
                <th>Ano:</th>
                <td>{{ evento.data ? evento.data|date('Y') : '' }}</td>
            </tr>
            <tr>
                <th>Data do Evento:</th>
                <td>{{ evento.data ? evento.data|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Local:</th>
                <td>{{ evento.local }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="container text-center my-3">
        <div class="row my-2">
            <div class="col">
                <a href="{{ path('app_lote_index') }}" class="btn btn-outline-primary w-100">
                    <i class="bi bi-card-checklist"></i>
                    Lotes
                </a>
            </div>
        </div>
        <div class="row my-2">
            <div class="col">
                <a class="btn btn-outline-primary w-100" href="{{ path('app_evento_index') }}">
                    <i class="bi bi-arrow-left-circle mx-2"></i>
                    Voltar
                </a>
            </div>
            <div class="col">
                <a class="btn btn-primary w-100" href="{{ path('app_evento_edit', {'id': evento.id}) }}">
                    <i class="bi bi-pencil-square"></i>
                    Editar
                </a>
            </div>
        </div>
    </div>

{% endblock %}
